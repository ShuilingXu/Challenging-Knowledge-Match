package com.matrixlive.screen;

/** Content modes that a screen renderer can resolve from the display state. */
public enum ScreenDisplayMode {
  LOBBY,
  TEMPLATE,
  QUESTION,
  RESULT,
  SCOREBOARD,
  LEADERBOARD,
  WINNERS,
  ANNOUNCEMENT,
  FILE
}
